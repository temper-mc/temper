[package]
name = "temper-performance"
description = "Stores all performance variables and calculates it."
edition.workspace = true
version.workspace = true

[dependencies]
sysinfo = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true

[lib]
test = false
doctest = false
